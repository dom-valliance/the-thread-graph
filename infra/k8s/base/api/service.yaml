apiVersion: v1
kind: Service
metadata:
  name: api
  namespace: valliance-graph
  labels:
    app.kubernetes.io/name: api
    app.kubernetes.io/part-of: valliance-graph
spec:
  type: ClusterIP
  ports:
    - port: 8000
      targetPort: 8000
      protocol: TCP
  selector:
    app.kubernetes.io/name: api
