apiVersion: v1
kind: Service
metadata:
  name: web
  namespace: valliance-graph
  labels:
    app.kubernetes.io/name: web
    app.kubernetes.io/part-of: valliance-graph
spec:
  type: ClusterIP
  ports:
    - port: 3000
      targetPort: 3000
      protocol: TCP
  selector:
    app.kubernetes.io/name: web
